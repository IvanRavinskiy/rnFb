import {StyleSheet} from 'react-native';

export const LoginStyles = StyleSheet.create({
  main: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  avatarImage: {
    width: 150,
    height: 150,
    borderRadius: 75,
  },
});
